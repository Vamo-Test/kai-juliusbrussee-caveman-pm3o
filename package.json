{
  "name": "kai-juliusbrussee-caveman-pm3o",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A lightweight, secure JSON API for monitoring and mitigating server-side vulnerabilities.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.30.0"
  }
}
